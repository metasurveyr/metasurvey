# Read a RecipeWorkflow from a JSON file

Read a RecipeWorkflow from a JSON file

## Usage

``` r
read_workflow(file)
```

## Arguments

- file:

  Character file path

## Value

A RecipeWorkflow object

## Examples

``` r
if (FALSE) { # \dontrun{
wf <- read_workflow("my_workflow.json")
} # }
```
