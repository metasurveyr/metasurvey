# Increment download counter (generic)

Increment download counter (generic)

## Usage

``` r
api_track_download(type, id)
```

## Arguments

- type:

  `"recipe"` or `"workflow"`

- id:

  Object ID
